# 公众号写作工作流

> 标准化的公众号文章创作流程，确保每篇文章风格统一、格式规范、高效产出。

---

## Skill 分工

| 环节 | 负责 Skill | 职责 |
|------|-----------|------|
| 写作 + 质检 | **wechraft-writer** | 风格选择、素材采集、文章撰写、四层自检 |
| 配图生成 | **ai-wechat-publisher** | `image_gen.py` 生成配图 |
| 上传发布 | **baoyu-post-to-wechat** | `wechat_api.py` 上传图片，`publish.py` 发布草稿箱 |

三个 skill 串联完成全流程。wechraft-writer 是主控，调用另外两个完成配图和发布。

---

## 两种输入模式

根据用户提供的内容不同，走不同的路径：

### 模式 A：从零创作

**触发**：用户提供主题（「写一篇关于 XX 的文章」）

```
用户提供主题 → 定内容类型 → 选写作风格 → 搜索素材 → 撰写 → 质检 → 配图 → 发布
```

**特点**：需要搜索素材环节，写作时间较长（约 10-17 分钟）

### 模式 B：改写已有草稿

**触发**：用户提供完整草稿（粘贴全文 / 丢文件 / 丢链接）

```
用户提供草稿 → 定内容类型 → 识别原稿风格 → 搜索补充素材 → 按风格改写 → 质检 → 配图 → 发布
```

**特点**：基于原稿搜索相关素材补充，改写为主（约 10-17 分钟）

**改写核心动作**：
- 结构重组（正式编号 → emoji 标记 + 口语转场）
- bullet point / 表格 → 散文叙述
- 禁用标点清理（—— → 逗号，： → 逗号，"" → 「」）
- 补充口语化表达（≥ 8-10 个）
- 补充自嘲（≥ 1 处）
- 补充文化升维（≥ 1 处）
- 替换结尾（营销互动 → 标准结尾块）
- 添加 3 个 IMAGE_N 标记

---

## 创作流程（7 步）

### Step 0.5：检查历史表现

在开始创作前，检查已发布文章的数据表现，优化选题和策略。

```bash
# 同步最新数据
NO_PROXY=api.weixin.qq.com HTTP_PROXY= HTTPS_PROXY \
py ai-wechat-publisher/scripts/wechat_analytics.py sync

# 生成分析报告
py ai-wechat-publisher/scripts/analyze_performance.py report --output output/performance_report.md

# 输出关键洞察
py ai-wechat-publisher/scripts/analyze_performance.py insights
```

如果有 `output/performance_report.md`，参考其中的规律：
- 哪种内容类型阅读量最高
- 哪种风格表现更好
- 标题特征（带数字/不带数字）的表现差异

---

### Step 1：确定内容类型

先确定文章类型，再选写作风格。

| 类型 | 定位 | 核心目标 | 搜索侧重点 |
|------|------|----------|-----------|
| **热点解读型** | 新鲜事快速反应 | 帮读者「看懂」发生了什么 | 新闻源、多方观点、背景信息 |
| **案例复盘型** | 项目/事件深度拆解 | 还原过程 + 提炼可复制经验 | 案例详情、数据、时间线、访谈 |
| **观点思考型** | 个人见解输出 | 给读者「新角度」启发 | 相关讨论、对立观点、理论支撑 |
| **工具测评型** | 产品深度体验 | 帮读者「决策」是否使用 | 官方资料、用户评价、竞品对比 |
| **实战教程型** | 可操作指南 | 带读者「学会」某技能 | 最佳实践、踩坑经验、官方文档 |

用户未指定时，根据主题自动推断类型。

---

### Step 2：选择写作风格

| 风格 | 适合场景 | 特点 |
|------|----------|------|
| 知鱼说（默认） | 行业分析、趋势解读、方法论 | emoji 章节标记，引用块开头结尾 |
| 卡兹克 | 产品体验、个人观点、叙事 | 口语化转场，无小标题，固定尾部 |

用户未指定时默认知鱼说。

---

### Step 3：搜索素材（仅模式 A）

**双引擎搜索**（推荐）：

```bash
py ai-wechat-publisher/scripts/research.py research "[主题]" --engine all --save output/research_notes.md
```

**引擎选择**：

| 场景 | 引擎 | 命令 |
|------|------|------|
| 全面覆盖 | 双引擎 | `--engine all` |
| 综合搜索/新闻 | Tavily | `--engine tavily` |
| 语义搜索/学术 | Exa | `--engine exa` |

保存素材到 `output/research_notes.md`。

**预估耗时**：1-2 分钟

---

### Step 4：撰写/改写文章

根据**内容类型 + 写作风格**组合创作，保存到 `output/article.md`。

#### 按内容类型的写作方法

**1. 热点解读型**

结构：
```markdown
# 标题（带时效性，如「刚刚」「今天」）

> 🐟 知鱼说：一句话定调

[🔥 事件速览] — 5-8 句话说清楚前因后果
---
[📊 多方反应] — 官方怎么说？网友怎么说？利益方怎么说？
<!-- IMAGE_1: 新闻场景可视化 -->
---
[💡 背后逻辑] — 为什么会发生？深层原因是什么？
<!-- IMAGE_2: 逻辑关系图 -->
---
[🎯 会怎么演变] — 接下来可能的发展，理性推测
---
[⚡ 对你我意味着什么] — 读者该关注还是无视
```

搜索关键词：`[事件] 最新进展 官方回应 专家解读`

---

**2. 案例复盘型**

结构：
```markdown
# 标题（带成果数字，如「3个月涨粉10万」）

> 🐟 知鱼说：这个案例的核心启示

[📋 项目背景] — 为什么做？目标是什么？资源有哪些？
---
[⏱️ 时间线] — 关键节点 + 当时的决策逻辑
<!-- IMAGE_1: 时间线/流程图 -->
---
[🎯 关键动作拆解] — 做对了哪3件事？为什么有效？
<!-- IMAGE_2: 核心策略可视化 -->
---
[💥 踩过的坑] — 哪里差点翻车？怎么解决的？
---
[🔥 可复用的经验] — 抽象成方法论，读者能直接用
```

搜索关键词：`[案例] 复盘 经验 数据 访谈 项目详情`

---

**3. 观点思考型**

结构：
```markdown
# 标题（带反直觉或鲜明立场）

> 🐟 知鱼说：我的核心观点

[💭 灵感来源] — 从什么事件/观察引发这个思考
---
[🤔 我的观点] — 清晰表达立场，不模棱两可
<!-- IMAGE_1: 观点概念可视化 -->
---
[🔄 为什么这么想] — 个人经历 + 对立观点分析 + 理论支撑
<!-- IMAGE_2: 对比/逻辑图 -->
---
[⚠️ 这个观点的边界] — 什么情况下不成立？保持理性
---
[🎯 读者可以怎么用] — 把抽象观点变成可行动的建议
```

搜索关键词：`[主题] 不同观点 争议 讨论 研究 反对意见`

---

**4. 工具测评型**

结构：
```markdown
# 标题（带结论，如「值得/不值得」）

> 🐟 知鱼说：一句话结论

[🔍 为什么要测] — 这个工具为什么值得关注？
---
[📋 核心功能拆解] — 3个核心功能逐一体验
<!-- IMAGE_1: 功能截图/界面 -->
---
[⚡ 真实使用场景] — 具体任务中用得怎么样？和XX比呢？
<!-- IMAGE_2: 对比表格/使用场景 -->
---
[🚫 劝退提醒] — 谁不适合？哪些坑要注意？
---
[💡 使用建议] — 怎么用效果最好？免费版够用吗？
```

搜索关键词：`[工具名] 测评 体验 对比 缺点 用户评价`

---

**5. 实战教程型**

结构：
```markdown
# 标题（带结果，如「从零到XXX」）

> 🐟 知鱼说：这个技能的价值

[🎯 为什么要学] — 解决什么问题？学会后有什么收益？
---
[📦 准备工作] — 需要什么前置知识/工具？
<!-- IMAGE_1: 准备工作清单 -->
---
[🔥 核心步骤拆解] — 分步骤讲清楚，每步给「为什么」
<!-- IMAGE_2: 关键步骤示意 -->
---
[⚠️ 踩坑预警] — 新手最容易犯的3个错误
---
[🎁 进阶技巧] — 掌握基础后怎么提升？最佳实践
```

搜索关键词：`[技能] 教程 最佳实践 踩坑 经验 教程`

---

#### 按写作风格的结构模板

**知鱼说结构**：
```markdown
# 标题

> 🐟 知鱼说：[核心观点]

[🔥 发生了什么]
---
[📊 核心分析]
<!-- IMAGE_1: prompt -->
---
[💡 深入解读]
<!-- IMAGE_2: prompt -->
---
[🎯 我的判断]
<!-- IMAGE_3: prompt -->
---
[⚡ 怎么用]

> 🐟 **关于知鱼**
> 专注 AI 赛道的实战派...
```

**卡兹克结构**：
```markdown
# 标题

[具体事件切入，口语化推进，无小标题]
<!-- IMAGE_N: prompt -->
[固定尾部 + 署名]
```

**配图标记规则**：
- 3 张，位置 30% / 60% / 90%
- 格式 `<!-- IMAGE_N: 具体英文描述 -->`

**预估耗时**：3-6 分钟

---

#### 富文本格式规范

文章必须使用 Markdown 富文本格式，确保可读性和专业性。

**格式使用规则**：

| 格式类型 | Markdown 语法 | 使用场景 | 示例 |
|---------|--------------|---------|------|
| **加粗** | `**文本**` | 核心观点、关键词、重要数据 | **GPT-4 的上下文窗口达到 128K** |
| *斜体* | `*文本*` | 强调、术语首次出现 | 这就是所谓的 *context switching*（上下文切换） |
| ``代码`` | `` `代码` `` | 命令、函数名、配置项 | 使用 ``pip install`` 安装 |
| 代码块 | ``` ``` \n 代码 \n ``` ``` | 完整代码示例、配置文件 | 见下方示例 |
| > 引用 | `> 引用内容` | 引用他人观点、官方说明、重要提示 | > OpenAI 官方文档指出... |
| [链接](url) | `[文本](url)` | 参考资料、相关阅读 | [Claude 官网](https://claude.ai) |
| 无序列表 | `- 项目` | 并列要点、列举 | 见本表 |
| 有序列表 | `1. 项目` | 步骤、优先级 | 见本表 |
| --- | `---` | 分隔不同章节 | 上下文分隔 |

**英文术语规范**：
- 英文术语首次出现时，必须在括号中添加中文翻译
- 示例：*prompt engineering*（提示词工程）、*hallucination*（幻觉问题）
- 例外：纯代码、函数名、已广泛认知的缩写（AI、API、URL等）

**代码块规范**：

````
\```python
# 指定语言，带语法高亮
def hello_world():
    print("Hello, World!")
\```
````

````
\```bash
# 命令行示例
npm install -g @anthropic-ai/claude-code
\```
````

**引用使用场景**：
- 引用官方文档、权威资料
- 突出重要提示、注意事项
- 引用他人观点进行讨论

**加粗使用场景**：
- 文章核心观点
- 关键数据、数字
- 重要结论
- 首次出现的关键术语

**格式使用原则**：
- ✅ 核心观点必须加粗
- ✅ 代码相关必须用代码块或行内代码
- ✅ 引用资料必须用引用格式
- ✅ 重要提示用引用块
- ✅ 主标题和副标题内容必须不同，角度要有差异
- ✅ 英文术语首次出现加括号附中文翻译
- ❌ 避免过度使用加粗（不超过全文 10%）
- ❌ 不要用加粗代替标题
- ❌ 代码块必须指定语言类型

**内容写作原则**：

| 原则 | 要求 | 示例 |
|------|------|------|
| 深度 | 不只是描述现象，要分析本质、挖掘原因 | 不只说「AI 很火」，要分析为什么火、影响是什么 |
| 通俗易懂 | 用生活化的比喻解释复杂概念 | 用「像一本能检索的百科全书」解释 LLM |
| 具体案例 | 用真实场景说明抽象观点 | 用产品经理用 AI 的案例说明「会提问」的重要性 |
| 逻辑清晰 | 论点有论据支撑，推导过程清晰 | 观点→案例→分析→结论 |
| 避免说教 | 分享经验而非教导读者 | 「我发现这样做很有效」而非「你必须这样做」 |

**深度与通俗的平衡**：
- ❌ 错误：只有深度，满篇术语，读者看不懂
- ❌ 错误：追求通俗，内容浅薄，没有价值
- ✅ 正确：用通俗的语言讲有深度的内容，让读者「能看懂且有所得」

**标题规范**：

| 类型 | 规范 | 示例 |
|------|------|------|
| 主标题 `# 标题` | 吸引眼球，可带数字、反直觉、时效性 | # 刚刚，Claude 4.7 发布了 |
| 副标题 `> 知鱼说：xxx` | 一句话提炼核心观点，与主标题不同角度 | > 🐟 知鱼说：不是更强的模型，是更会「想」的模型 |

❌ **错误示例**（主副标题一样）：
```markdown
# Claude 4.7 发布了

> 🐟 知鱼说：Claude 4.7 发布了
```

✅ **正确示例**（角度不同）：
```markdown
# Claude 4.7 发布了

> 🐟 知鱼说：不是更强的模型，是更会「想」的模型
```

---

### Step 5：四层质检

写完立即自检，不通过就修，修完重检该层。

#### L1 硬性规则（0 容忍）

| 检查项 | 要求 |
|--------|------|
| 禁用词 | ❌ 说白了、意味着什么、本质上、换句话说、不可否认、综上所述、值得注意的是 |
| 禁用标点 | ❌ 冒号：、破折号——、双引号"" → 用「」 |
| 结构套话 | ❌ 让我们来看看、随着...的发展、在当今...的时代 |
| 工具名 | ✅ 全部具体名称，无「AI工具」「某个模型」 |
| 编造例子 | ❌ 无「比如有一次...」假设场景 |
| 代码块语言 | ✅ 必须指定语言类型（\```python、\```bash 等） |
| 引用格式 | ✅ 引用资料必须用 > 引用块，不得混入正文 |
| 主副标题 | ✅ 内容必须不同，角度要有差异 |

#### L2 风格一致性

| 检查项 | 通过标准 |
|--------|---------|
| 开头 | 从具体事件切入，非宏大叙事 |
| 节奏 | ≥ 3 处单句独立成段，长短句交替 |
| 口语化 | ≥ 8-10 个不同口语表达，≥ 1 处自嘲 |
| 标点二次确认 | 全文无冒号、破折号、双引号 |
| 富文本格式 | 核心观点**加粗**、代码用``代码块``、引用用>引用块 |

#### L3 内容质量

| 检查项 | 通过标准 |
|--------|---------|
| 观点支撑 | 每个核心观点有数据/场景支撑 |
| 知识输出 | 「顺手掏」，非教科书科普 |
| 文化升维 | ≥ 1 处连接更大的参照物 |
| 对立面 | 理解对方立场后再给观点 |

#### L4 活人感

| 检查项 | 通过标准 |
|--------|---------|
| 温度感 | 体感记忆式情绪，非知识性描述 |
| 独特性 | 换个 AI 写不出的角度 |
| 姿态 | 真人聊天，非导师教学生 |
| 心流 | 从头到尾无断点 |

**质检输出格式**：
```
## 质检报告

**L1 硬性规则** ✅/❌ — X处命中（已修复）
**L2 风格一致性** ✅/❌ — 口语化 X 个
**L3 内容质量** ✅/❌
**L4 活人感** ✅/❌

**总评**：4层全部通过 / X层需要返工
```

**预估耗时**：2-3 分钟

---

### Step 6：配图生成 + 质量校验

解析文章中 `<!-- IMAGE_N: ... -->` 标记，逐张生成。

```bash
py ai-wechat-publisher/scripts/image_gen.py generate "英文prompt" --output output/image_N.jpg --size 16:9
```

**质量校验**（每张图必须检查）：

| 指标 | 合格标准 | 不合格处理 |
|------|---------|-----------|
| 文件大小 | > 100KB | 重新生成 |
| 比例 | 16:9 | 检查 --size 参数 |

```bash
# 快速检查文件大小
ls -la output/image_*.jpg
```

失败时使用 fallback：
```bash
py ai-wechat-publisher/scripts/image_gen.py fallback "简短描述" --output output/image_N.jpg
```

**预估耗时**：1-2 分钟（3 张并行约 20 秒）

---

### Step 7：上传 + 发布

#### 6.1 上传配图

```bash
py ai-wechat-publisher/scripts/wechat_api.py batch-upload output/image_1.jpg output/image_2.jpg output/image_3.jpg
```

**注意**：微信 API 需要绕过本地代理，命令前加：
```bash
NO_PROXY=api.weixin.qq.com HTTP_PROXY= HTTPS_PROXY= py ...
```

记录返回的每张图片 URL。

#### 6.2 发布到草稿箱

```bash
NO_PROXY=api.weixin.qq.com HTTP_PROXY= HTTPS_PROXY \
py ai-wechat-publisher/scripts/publish.py output/article.md \
  --replacements "IMAGE_1:微信URL1" "IMAGE_2:微信URL2" "IMAGE_3:微信URL3" \
  --cover-index 1
```

`--cover-index 1` 表示用第一张图当封面。

#### 6.3 用户确认

输出 media_id，用户去草稿箱检查后可要求直接发布：
```bash
py ai-wechat-publisher/scripts/wechat_api.py publish <media_id>
```

**预估耗时**：1-2 分钟

---

## 时间预估

| 步骤 | 模式 A（从零） | 模式 B（改写） |
|------|---------------|---------------|
| 定内容类型 | < 1 min | < 1 min |
| 选写作风格 | < 1 min | < 1 min |
| 搜索素材 | 1-2 min | 1-2 min |
| 撰写/改写 | 4-6 min | 3-5 min |
| 四层质检 | 2-3 min | 2-3 min |
| 配图生成 | 1-2 min | 1-2 min |
| 上传发布 | 1-2 min | 1-2 min |
| **合计** | **10-17 min** | **10-17 min** |

---

## 常见问题排查

| 问题 | 原因 | 解决 |
|------|------|------|
| 微信 API 报 `invalid ip ... not in whitelist` | 当前 IP 不在公众号白名单 | mp.weixin.qq.com → 设置与开发 → 基本配置 → IP 白名单，添加报错中的 IP |
| 微信 API 报 `ProxyError` / `ConnectionReset` | 本地代理拦截了微信请求 | 命令前加 `NO_PROXY=api.weixin.qq.com HTTP_PROXY= HTTPS_PROXY=` |
| 配图文件 < 100KB | 生成失败或质量异常 | 重新执行 generate 命令 |
| access_token 报错 | token 过期 | 脚本会自动刷新，检查 .env 中 WECHAT_APP_ID 和 WECHAT_APP_SECRET 是否正确 |
| 草稿箱创建失败 | HTML 超 20000 字符 | 精简内容后重试 |

---

## 文件结构

```
output/
├── article.md              # 文章内容
├── image_1.jpg             # 配图1（30%）
├── image_2.jpg             # 配图2（60%）
├── image_3.jpg             # 配图3（90%）
├── research_notes.md       # 研究笔记（可选，仅模式A）
├── published_articles.json # 发布档案（自动记录）
└── performance_report.md   # 性能分析报告（可选）
```

---

*最后更新：2026-05-09*
